# explorenyc-db
This repository holds conventions and issues for ExploreNYC's relational database operations. The user schema has explanations for the stops json format, which is used communicating from the route generator to the database to store trip information. It also has a table spec for the trip table, including row names and data types. 
